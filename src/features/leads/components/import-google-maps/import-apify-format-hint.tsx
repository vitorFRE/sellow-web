export function ImportApifyFormatHint() {
  return (
    <div role="note">
      <p className="text-sm font-medium text-stat-value">Formato Apify</p>
      <p className="mt-2 text-sm leading-relaxed text-stat-muted">
        O JSON segue o padrão de exportação do Apify para dados do Google Maps —
        por exemplo, a saída de um actor de scraping. Cole o resultado do run ou
        carregue um arquivo nesse formato.
      </p>
    </div>
  )
}
